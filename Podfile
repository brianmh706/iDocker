workspace 'iDocker.xcworkspace'
project 'macOS/macOS.xcodeproj'

target 'macOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire'

  target 'macOSTests' do
    inherit! :search_paths
    
  end

end
